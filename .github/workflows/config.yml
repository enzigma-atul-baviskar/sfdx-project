
name: Master

on: [push]


jobs: 
 build:
   runs-on: ubuntu-latest
   steps:
      - name: Download CLI
        run: 
          echo "This is DIR::" $(pwd)
          echo "T" $(pwd)/assests/sfdx-Test
           mkdir  assests/sfdx-Test
          

